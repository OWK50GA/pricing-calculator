# Full URL of the API server, no trailing slash.
# In local development this is omitted — the Vite proxy handles /api → localhost:3001.
# For production deployments, set this to your deployed server URL.
VITE_API_URL=https://your-api-server.com
